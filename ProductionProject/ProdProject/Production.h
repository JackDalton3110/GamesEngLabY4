#pragma once
#include <algorithm>
#include <vector>

class Production {
public:
	int someMethod(int a);
	int VectorSizeTest(std::vector<int> v);
	bool CheckNoDoubles(std::vector<int> v);
	bool NumRange(std::vector<int> v);
};
